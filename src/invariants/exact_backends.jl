# Private invariant-owner exact backend extensions.
#
# Presentation-native exact backends should live here so they compose through
# the shared `InvariantCore` hook surface instead of depending on a specific
# workflow owner. No invariant-owner exact backends are implemented yet.
